<p style="color: green"><%= notice %></p>

<h1>Lecturer units</h1>

<div id="lecturer_units">
  <% @lecturer_units.each do |lecturer_unit| %>
    <div id="<%= dom_id lecturer_unit %>">
      <p>
        <strong>Course:</strong>
        <%= Course.find_by(id: lecturer_unit.course_id)&.course_name || 'N/A' %>
      </p>
    </div>

    <%# Example link, uncomment if needed %>
    <%# <p>
    <%#   <%= link_to "Show this lecturer unit", lecturer_lecturer_units %>
    <%# </p> %>
  <% end %>
</div>

<%= link_to "New lecturer unit", new_lecturer_lecturer_unit_path %>
