<%= form_with(url: student_students_courses_path, model: students_course, class: "needs-validation", novalidate: true) do |form| %>
  <% if students_course.errors.any? %>
    <div class="alert alert-danger">
      <h2><%= pluralize(students_course.errors.count, "error") %> prohibited this students_course from being saved:</h2>

      <ul>
        <% students_course.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <%= form.label :lecturer_unit_id, class: "d-block" %>
    <%= form.collection_select :lecturer_unit_id, @available_courses, :id, :course_name_from_lecturer_unit, { prompt: 'Select a Course' }, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.submit "Save", class: "btn btn-primary" %>
  </div>
<% end %>
