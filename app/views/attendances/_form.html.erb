<%= form_with(model: attendance) do |form| %>
  <% if attendance.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(attendance.errors.count, "error") %> prohibited this attendance from being saved:</h2>

      <ul>
        <% attendance.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :student_id, style: "display: block" %>
    <%= form.text_field :student_id %>
  </div>

  <div>
    <%= form.label :lecturer_unit_id, style: "display: block" %>
    <%= form.text_field :lecturer_unit_id %>
  </div>

  <div>
    <%= form.label :attendance_date, style: "display: block" %>
    <%= form.date_field :attendance_date %>
  </div>

  <div>
    <%= form.label :present, style: "display: block" %>
    <%= form.check_box :present %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
